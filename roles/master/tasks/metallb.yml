---
# Limpieza completa de MetalLB
- name: Remove existing MetalLB installation
  shell: |
    kubectl delete namespace metallb-system --ignore-not-found=true
  ignore_errors: yes

- name: Wait for cleanup
  shell: |
    kubectl wait --for=delete namespace/metallb-system --timeout=60s
  ignore_errors: yes

# Instalación básica de MetalLB
- name: Create metallb namespace
  command: kubectl create namespace metallb-system

- name: Create memberlist secret
  shell: |
    kubectl create secret generic -n metallb-system memberlist \
    --from-literal=secretkey="$(openssl rand -base64 128)"

- name: Download and modify MetalLB manifest
  shell: |
    # Descargar el manifiesto
    wget https://raw.githubusercontent.com/metallb/metallb/v0.13.7/config/manifests/metallb-native.yaml -O /tmp/metallb-no-webhooks.yaml
    # Eliminar las secciones de webhook (más específico)
    sed -i '/apiVersion: admissionregistration.k8s.io/,$d' /tmp/metallb-no-webhooks.yaml

- name: Apply modified MetalLB manifest
  command: kubectl apply -f /tmp/metallb-no-webhooks.yaml --validate=false

- name: Wait for MetalLB deployment
  shell: |
    kubectl -n metallb-system rollout status deployment controller
  register: controller_status
  retries: 30
  delay: 10
  until: controller_status.rc == 0

# Configuración de MetalLB
- name: Create MetalLB config
  copy:
    dest: /tmp/metallb-config.yaml
    content: |
      apiVersion: metallb.io/v1beta1
      kind: IPAddressPool
      metadata:
        name: first-pool
        namespace: metallb-system
      spec:
        addresses:
        - {{ metallb_ip_range }}
      ---
      apiVersion: metallb.io/v1beta1
      kind: L2Advertisement
      metadata:
        name: l2-advert
        namespace: metallb-system
      spec:
        ipAddressPools:
        - first-pool

- name: Apply MetalLB configuration
  command: kubectl apply -f /tmp/metallb-config.yaml --validate=false

- name: Show MetalLB status
  shell: |
    kubectl get pods -n metallb-system
  register: metallb_status 